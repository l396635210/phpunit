{% extends 'base.html.twig' %}
{% block body %}
    {% include('_partials/_newDinoForm.html.twig') %}
    <h3>Enclosures</h3>
    <table class="table-enclosures">
        <tbody>
            {% for item in listData %}
            <tr id="enclosure-{{ item.id }}">
                <td>
                    Enclosure #{{ item.id }}
                </td>
                <td>
                    Contains <strong>{{ item.dinosaurCount }}</strong> dinosaur(s)
                </td>
                <td>
                    {% if item.securityActive %}
                        🔒 Security active!
                        {% else %}
                        <button class="button button-alarm">Sound alarm !!!</button>
                    {% endif %}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}